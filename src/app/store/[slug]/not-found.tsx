export default function StoreNotFound() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-[#0a0d14] px-6 text-center text-white">
      <h1 className="text-2xl font-bold">Store not available</h1>
      <p className="mt-2 max-w-sm text-sm text-white/60">
        This landing page does not exist or is no longer active. Please check the
        link or QR code and try again.
      </p>
    </main>
  );
}
